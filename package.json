{
  "name": "sdkv2",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "workspaces": [
    "1-base-layer",
    "2-definition-layer",
    "3-platform-layer/*",
    "4-xdapp-layer"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@types/jest": "^29.5.3",
    "@types/node": "^20.4.1",
    "jest": "^29.6.2",
    "ts-jest": "^29.1.1",
    "typescript": "^5.1.6"
  }
}
